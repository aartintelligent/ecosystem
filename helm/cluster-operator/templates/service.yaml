apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-operator.fullname" . }}
  labels:
    {{- include "cluster-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-operator.selectorLabels" . | nindent 4 }}
