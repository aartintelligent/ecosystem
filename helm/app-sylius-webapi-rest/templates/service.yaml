apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-sylius-webapi-rest.fullname" . }}
  labels:
    {{- include "app-sylius-webapi-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-sylius-webapi-rest.selectorLabels" . | nindent 4 }}
