apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-datacenter.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-datacenter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-datacenter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
