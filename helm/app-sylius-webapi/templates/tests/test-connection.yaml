apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-sylius-webapi-rest.fullname" . }}-test-connection"
  labels:
    {{- include "app-sylius-webapi-rest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-sylius-webapi-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
