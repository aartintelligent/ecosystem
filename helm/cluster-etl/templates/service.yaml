apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-etl.fullname" . }}
  labels:
    {{- include "cluster-etl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-etl.selectorLabels" . | nindent 4 }}
