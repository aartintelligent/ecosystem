apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-etl.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-etl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-etl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
