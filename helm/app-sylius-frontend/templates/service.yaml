apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-sylius-frontend.fullname" . }}
  labels:
    {{- include "app-sylius-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-sylius-frontend.selectorLabels" . | nindent 4 }}
