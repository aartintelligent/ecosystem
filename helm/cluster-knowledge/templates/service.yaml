apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-knowledge.fullname" . }}
  labels:
    {{- include "cluster-knowledge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-knowledge.selectorLabels" . | nindent 4 }}
