apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-sylius-backend.fullname" . }}
  labels:
    {{- include "app-sylius-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-sylius-backend.selectorLabels" . | nindent 4 }}
