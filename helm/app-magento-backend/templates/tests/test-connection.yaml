apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-magento-backend.fullname" . }}-test-connection"
  labels:
    {{- include "app-magento-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-magento-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
