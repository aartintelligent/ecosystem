apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-tensorflow.fullname" . }}
  labels:
    {{- include "cluster-tensorflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-tensorflow.selectorLabels" . | nindent 4 }}
