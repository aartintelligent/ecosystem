apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-quality.fullname" . }}
  labels:
    {{- include "cluster-quality.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-quality.selectorLabels" . | nindent 4 }}
