apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-quality.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-quality.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-quality.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
