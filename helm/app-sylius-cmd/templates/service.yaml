apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-sylius-cmd.fullname" . }}
  labels:
    {{- include "app-sylius-cmd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-sylius-cmd.selectorLabels" . | nindent 4 }}
