apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-middleware.fullname" . }}
  labels:
    {{- include "cluster-middleware.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-middleware.selectorLabels" . | nindent 4 }}
