apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-vault.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-vault.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-vault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
