apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-vault.fullname" . }}
  labels:
    {{- include "cluster-vault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-vault.selectorLabels" . | nindent 4 }}
