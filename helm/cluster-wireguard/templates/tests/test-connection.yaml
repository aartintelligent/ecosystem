apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-wireguard.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-wireguard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-wireguard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
