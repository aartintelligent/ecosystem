apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-crawler.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-crawler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-crawler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
