apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-crawler.fullname" . }}
  labels:
    {{- include "cluster-crawler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-crawler.selectorLabels" . | nindent 4 }}
